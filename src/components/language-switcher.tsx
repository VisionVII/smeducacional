'use client';

export function LanguageSwitcher({ className }: { className?: string }) {
  // i18n desativado: esconder seletor de idioma, mas mantemos a assinatura para reativação futura
  void className;
  return null;
}

export function FloatingLanguageSwitcher() {
  // i18n desativado: esconder seletor flutuante
  return null;
}
